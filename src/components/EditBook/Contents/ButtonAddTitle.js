import React from "react"

const ButtonAddTitle = ({ children }) => (
  <button className="md:w-full w-40 text-sm border border-gray-400 text-gray-800 rounded">
    {children}
  </button>
)

export default ButtonAddTitle
